# Three-qubit line topology

Physical connectivity:

```text
0 -- 1 -- 2
```

Shared benchmark suites live under `suites/`. Each experiment has its own
configuration and outputs under `experiments/`.
